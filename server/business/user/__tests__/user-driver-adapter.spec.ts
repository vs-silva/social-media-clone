import {describe, it} from "vitest";
//import {faker} from "@faker-js/faker";
//import request from "supertest";
//import type {RequestCreateUserDTO} from "~/server/business/user/core/dto/request-create-user.dto";

describe('User driver adapter tests', () => {

    /*const idRegex = /\b[0-9a-f]{24}\b/;

    const fakePassword = faker.internet.password();

    const fakeRequestCreateUserDTO: RequestCreateUserDTO = {
      username: faker.internet.userName(),
      name: faker.person.fullName(),
      email: faker.internet.email(),
      password: fakePassword,
      repeatPassword: fakePassword
    }; */

    it.todo('post to api/auth/register should create a new user and return the newly created user data');

});
